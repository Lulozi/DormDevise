import 'package:flutter/material.dart';

class OpenDoorPage extends StatelessWidget {
  const OpenDoorPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('opendoor Page')));
  }
}
